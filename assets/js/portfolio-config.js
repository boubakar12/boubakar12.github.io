window.PORTFOLIO_SITE_CONFIG = window.PORTFOLIO_SITE_CONFIG || {
  // Add your Google Maps JavaScript API key here to enable the 3D journey map.
  // Keep the key restricted to your portfolio domains in Google Cloud Console.
  googleMapsApiKey: '',

  // Optional: add a Map ID if you want a custom Google Cloud map style.
  googleMapsMapId: ''
};
